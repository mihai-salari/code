program null_pointer;
#include("stdlib.hhf")

static
	n1: int8 := 0;

begin null_pointer;
	
	if (al == 0) then
		stdout.put("Error: NULL pointer encountered", nl);
	else
		stdout.put("Pointer is valid", nl);
		stdout.put("al = ", al, nl);
		mov(n1, al);
		stdout.put("al = ", al, nl);
		mov(9, al);
		stdout.put("al = ", al, nl);
	endif;

end null_pointer;