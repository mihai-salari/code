#include <stdio.h>
#include <stdbool.h>

int main(int argc, const char * argv[]) {
    bool is_true = true;
    
    if (is_true) {
        printf("It is true\n");
    } else {
        printf("It is not true\n");
    }
    
    return 0;
}
