#include <stdio.h>

int main(int argc, const char * argv[]) {
    int age = 38;
    
    if (age >= 40) {
        printf("Middle age");
    } else if (age >= 20) {
        printf("Going to be middle age");
    }
    
    printf("\n");
    
    return 0;
}
