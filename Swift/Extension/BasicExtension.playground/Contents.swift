import Cocoa

// Extension can't override an existing member (but it can overload an existing method)
// Extension can't declare a stored property (but it can declare a computed property)
// Extension of a class can't declare a designated initializer or a deinitializer (but it can declare a convenience initializer)

