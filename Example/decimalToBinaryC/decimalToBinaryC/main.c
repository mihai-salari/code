#include <stdio.h>
#include <stdbool.h>

int main(int argc, const char * argv[]) {
    bool isComplete = false;
    int decimal = 10;
    
    
    
    while (!isComplete) {
        
    }
    
    return 0;
}
